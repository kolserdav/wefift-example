import { RevokedCertificate } from "@peculiar/asn1-x509";
import { Extension } from "./extension";
import { AsnData } from "./asn_data";
export declare enum X509CrlReason {
    unspecified = 0,
    keyCompromise = 1,
    cACompromise = 2,
    affiliationChanged = 3,
    superseded = 4,
    cessationOfOperation = 5,
    certificateHold = 6,
    removeFromCRL = 8,
    privilegeWithdrawn = 9,
    aACompromise = 10
}
export declare class X509CrlEntry extends AsnData<RevokedCertificate> {
    serialNumber: string;
    revocationDate: Date;
    reason?: X509CrlReason;
    invalidity?: Date;
    extensions: Extension[];
    constructor(raw: BufferSource);
    constructor(serialNumber: string, revocationDate: Date, extensions: Extension[]);
    protected onInit(asn: RevokedCertificate): void;
}
